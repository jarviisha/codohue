# Build output — Vite produces this; embedui Go builds expect dist/ to exist
dist/

# Vite cache
.vite/

# Node
node_modules/

# TypeScript incremental build cache
*.tsbuildinfo

# Local env overrides — .env.example is tracked, real values stay local
.env
.env.local
.env.*.local

# Editor
.DS_Store
