// Stub. Real implementation lands in Phase 1.5 alongside services/auth.ts.
export default function LoginPage() {
  return (
    <main className="min-h-screen bg-base flex items-center justify-center px-6">
      <div className="w-90 border border-default rounded-sm bg-surface p-6">
        <h1 className="text-xl font-semibold text-primary leading-tight mb-4">
          Sign in
        </h1>
        <p className="text-sm text-muted">
          <span className="font-mono text-muted">[PEND]</span> implementation pending.
        </p>
      </div>
    </main>
  )
}
