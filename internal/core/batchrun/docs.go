// Package batchrun owns shared contracts about batch_run_logs rows so the
// admin, compute, and embedder domains can refer to the same trigger source
// values without depending on each other.
package batchrun
